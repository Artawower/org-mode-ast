{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "launch",
      "name": "DEBUG Jest: link spec",
      "runtimeExecutable": "/opt/homebrew/bin/node",
      "program": "${workspaceFolder}/node_modules/.bin/jest",
      "args": [
        // "${fileBasenameNoExtension}",
        "--config",
        "jest.config.cjs",
        "-i",
        "${workspaceFolder}/src/parser/handlers/list.spec.ts",
        "--no-watch"
      ],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "sourceMaps": true,
      "disableOptimisticBPs": true,
      "protocol": "inspector"
    }
  ]
}
